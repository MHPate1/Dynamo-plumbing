[build]
  command = "npm run build"
  publish = "dist"

# Prerendered files at each path are served first; this only catches paths that
# don't have a static file, so client-side routing still works.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
